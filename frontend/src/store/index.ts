/** Server state is handled by TanStack Query. This module stays empty of duplicated client caches. */
export const store = {
  description: "No extra global store. Auth and language live in React context; server data uses TanStack Query.",
};
